#include <stdio.h>
#include <stdlib.h>

int main (){
	//vetor v que armazena quantidade aleatória terminados com 0
	//abaixo tem o limite da soma que é o valor que deve ser calculado para cada para de numeros
	//o numero que deve ter na saida é o que passa o limite do valor informado.
	//tamanho do vetor? - o 0
	int nums[99999], soma_par_num, limite,resp;
	while(nums[]
}
